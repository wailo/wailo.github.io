{
  "name": "v3_personal_page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:start": "docker run --rm --name peerjs -p 9000:9000 -d peerjs/peerjs-server",
    "dev:stop": "docker stop peerjs || true",
    "dev": "npm run dev:start && trap 'npm run dev:stop' EXIT; vite",
    "build": "npm run build:emscripten && vue-tsc -b && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -b master",
    "build:emscripten": "git -C node_modules/Compact-Flight-Simulator checkout 9eff8e8 && git -C node_modules/Compact-Flight-Simulator submodule update --init --recursive && ./node_modules/Compact-Flight-Simulator/build_wasm.sh && cp -a node_modules/Compact-Flight-Simulator/build_wasm_*/bin/flightsimulator* public/",
    "preinstall": "[ -z \"$ACCESS_TOKEN\" ] && set -a && . .env && set +a; git clone --recursive https://oauth2:$ACCESS_TOKEN@github.com/wailo/Compact-Flight-Simulator.git node_modules/Compact-Flight-Simulator"
  },
  "dependencies": {
    "monaco-editor-vue3": "^0.1.10",
    "peerjs": "^1.5.4",
    "vue": "^3.4.29",
    "vue-qr": "^4.0.9",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "autoprefixer": "^10.4.19",
    "gh-pages": "^6.3.0",
    "postcss": "^8.4.39",
    "prettier": "3.3.2",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.2.2",
    "vite": "^5.3.1",
    "vue-tsc": "^2.0.21"
  }
}
